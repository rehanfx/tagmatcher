{
  "name": "matcher-server",
  "version": "1.0.0",
  "description": "A fake data server with profile info",
  "main": "db.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "faker": "^3.0.1",
    "json-server": "^0.8.2",
    "lodash": "^3.10.1"
  }
}
